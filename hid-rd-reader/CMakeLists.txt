add_executable(hid-rd-reader main.cpp)
target_link_libraries(hid-rd-reader PRIVATE ${PROJECT_NAME})
if(NOT IS_TOPLEVEL_PROJECT)
    set_target_properties(hid-rd-reader PROPERTIES EXCLUDE_FROM_ALL TRUE)
endif()
